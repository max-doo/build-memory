# Knowledge Index & Routing Table (Memory Index)

> **Usage:** Before complex modifications or debugging, match triggers against the current task keywords or paths. If an L1 entry matches, read ONLY the corresponding ### section in `.memory/KNOWLEDGE.md`.

## L0 Global Rules (Already injected via AGENTS.md, do not read KNOWLEDGE)
| id | Rule Description | Location |
|---|---|---|
| `core-rules` | Core engineering constraints and package manager rules | `AGENTS.md` -> Project Structure / Tech Stack |

## L1 Knowledge Routing (Read corresponding KNOWLEDGE slice when triggered)
| id | triggers (Keywords / Path Globs) | KNOWLEDGE Section Anchor | Notes |
|---|---|---|---|
| `example-api` | `api/, fetch, http, timeout` | `### 1. API Request & Networking Guidelines` | Example routing entry (replace per project) |
